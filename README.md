# Online Bomberman

## Building and Running

Depencies: raylib for the client.

Run `make all` to build both the client and the server, then
the server can be started with `./build/server/server <port> <map_file>`, and
the client with `./build/client/client`.
